var people = ["Jon", "Jacob", "Jingle", "Heimer", "Schmidt"]
var alphabet = "abcdefghijklmnopqrstuvwxyz"

var lettersArr = alphabet.split("")
//console.log(lettersArr)

const newArr = people.concat(lettersArr);
//console.log(newArr)

var people = ["Jon", "Jacob", "Jingle", "Heimer", "Schmidt"]
var alphabet = "abcdefghijklmnopqrstuvwxyz"

function forception (people, alphabet){
   let newArray = []
   for(var i = 0; i <people.length; i++){
       
   }
        //push the people by index to newArray
       // for loop for alphabet here
       //push the alphabet by index to newArray and add the .toUpperCase() method to it.

   return newArray
}

console.log(forception(people, alphabet))